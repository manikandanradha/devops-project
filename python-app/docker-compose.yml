services:
  python-app:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    depends_on:
      - my-redis
  my-redis:
    image: redis:alpine
    volumes:
      - redis-data:/data
volumes:
  redis-data:

 